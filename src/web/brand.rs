/*
 * ========================================================================
 * 步骤1：收敛品牌图标的单一来源
 * ========================================================================
 * 目标：
 * 1) 把品牌 svg 文件 assets/favicon.svg 同时给侧边栏内联和 favicon 路由复用
 * 2) 编译期内联，避免运行时再去读文件
 * 3) 任何地方需要展示品牌 mark 都从这里取
 */
pub(crate) const BRAND_MARK_SVG: &str = include_str!("assets/favicon.svg");
